<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Add Area</title>

  <!-- Font Awesome for back arrow -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"/>

  <style>
    body {
      background-color: #f8f9fa;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .box {
      background-color:lavender;
      width: 60%;
      margin: 60px auto;
      padding: 40px;
      border-radius: 10px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    }

    h2 {
      text-align: center;
      color:purple;
      margin-bottom: 30px;
      font-size:30px;
      text-transform: uppercase;
    }

    .form-group {
      margin-bottom: 20px;
    }

    input[type="text"] {
      width: 100%;
      height: 48px;
      padding: 10px;
      font-size: 16px;
      border: 1px solid blueviolet;
      border-radius: 6px;
      box-sizing: border-box;
    }

    .alert {
      margin-bottom: 25px;
      padding: 15px;
      background-color: purple;
      color: #333;
      border-radius: 5px;
      text-align: center;
      font-weight: bold;
    }

    .btn-row {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-top: 10px;
    }

    .btn-orange {
      background-color: purple;
      color: #fff;
      font-weight: 600;
      width: 160px;
      height: 48px;
      border: none;
      border-radius: 5px;
      text-align: center;
      line-height: 48px;
      transition: background 0.3s;
      text-decoration: none;
      font-size: 16px;
    }

    .btn-orange:hover {
      background-color: purple;
    }

    @media (max-width: 768px) {
      .box {
        width: 90%;
      }

      .btn-row {
        flex-direction: column;
        align-items: center;
        gap: 12px;
      }

      .btn-orange {
        width: 100%;
      }
    }
  </style>
</head>
<body>

  <div class="box">
    <h2>Add New Area</h2>
    <form action="/areaadd" method="post">

      <div class="form-group">
        <input type="text" id="area_name" name="area_name" placeholder="Enter Area Name" required />
      </div>
      <!-- Buttons -->
      <div class="btn-row">
        <input type="submit" class="btn-orange" value="Add Area" />
      </div>
       <% if (typeof msg !== 'undefined') { %>
    <div style="color: green; margin-bottom: 10px;">
      <%= msg %>
    </div>
     <% } %>

    </form>
  </div>
</body>
</html>