<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Admin Dashboard</title>
   <style>
  body {
  margin: 0;
  padding: 0;
  font-family:Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif;
  background-image: url("image/back.jpg");
  background-size: cover;
  background-repeat: no-repeat;
  background-position: center;
  background-attachment: fixed;
  height: 100vh;
  overflow: hidden;
}
/* Navigation Bar */
.navigation {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background: linear-gradient(to right,#654ea3,#eaafc8); /* Purple-blue gradient */
  color: black;
  padding: 15px 30px;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
}

.navigation h3 {
  margin: 0 auto;
  font-size: 30px;
  font-weight: bold;
  margin-left: 550px;;
}

/* Profile Section */
.profile-container {
  position: relative;
  cursor: pointer;
}

.profile-icon {
  color: black;
  font-size: 40px;
}

.profile-menu {
  display: none;
  position: absolute;
  top: 45px;
  right: 0;
  background: white;
  color: #333;
  border: 1px solid #ccc;
  border-radius: 8px;
  box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2);
  width: 220px;
  padding: 15px;
  z-index: 1000;
}

.profile-menu p {
  margin: 6px 0;
  font-size: 14px;
}

.profile-menu a {
  text-decoration: none;
  color: #6a1b9a;
  font-weight: bold;
  margin-top: 10px;
  display: block;
}

/* Dashboard Layout */
.dashboard-container {
  display: flex;
  height: calc(100vh - 70px);
}

/* Sidebar */
.sidebar {
  width: 210px;
  color:white;
  padding-top: 20px;
  box-shadow: 2px 0 6px rgba(0, 0, 0, 0.15);
  overflow-y: auto;
}

.sidebar-menu {
  list-style: none;
  padding: 0;
  margin: 0;
}

.sidebar-menu li {
  padding: 14px 20px;
  font-size: 16px;
  display: flex;
  align-items: center;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.sidebar-menu li a {
  text-decoration: none;
  color:white;
  display: flex;
  gap: 8px;
  align-items: center;
}

.sidebar-menu li:hover {
    background-color:white;
   
  
}

.sidebar-menu li:hover a {
  color:purple;
  font-weight: bold;
  
}

/* Main Content Area */
.main-body {
  flex: 1;
  padding: 40px;
  color:black;
  overflow: auto;
  height: 100%;
  margin-top:-10x;
  
}

.iframe-wrapper {
  width: 100%;
  height: 100%;
  border-radius: 10px;
  overflow: hidden;
}

.iframe-wrapper iframe {
  width: 100%;
  height: 100%;
  border: none;
}

/* Scrollbar Removal */
.main-body::-webkit-scrollbar {
  width: 0;
  background: transparent;
}

/* Responsive Layout */
@media screen and (max-width: 991px) {
  .navigation {
    flex-direction: column;
    align-items: flex-start;
    padding: 10px 20px;
  }

  .navigation h3 {
    font-size: 24px;
    margin: 10px 0;
  }

  .dashboard-container {
    flex-direction: column;
    height: auto;
  }

  .sidebar {
    width: 100%;
    height: auto;
    position: relative;
    border-right: none;
    border-bottom: 1px solid #ccc;
  }

  .sidebar-menu li {
    justify-content: center;
    padding: 12px;
    font-size: 14px;
  }

  .main-body {
    padding: 25px 15px;
    height: auto;
  }

  .iframe-wrapper {
    height: 70vh;
  }

  .profile-icon {
    font-size: 30px;
  }

  .profile-menu {
    width: 90%;
    right: 5%;
  }
}

@media screen and (max-width: 600px) {
  .sidebar-menu li {
    font-size: 13px;
    padding: 10px;
  }

  .navigation h3 {
    font-size: 20px;
  }

  .main-body {
    padding: 20px;
  }

  .iframe-wrapper {
    height: 65vh;
  }
}
</style>

  <!-- Font Awesome & Fonts -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"/>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@500;700&display=swap" rel="stylesheet"/>
  <link rel="stylesheet" href="style.css">
</head>
<body>

  <!-- Top Navigation Bar -->
  <nav class="navigation">
    <div class="navbar-left">
      <h3>Admin Panel</h3>
    </div>
    <div class="navbar-right">
      <div class="profile-container" onclick="toggleProfileMenu()">
        <i class="fa fa-user-circle-o profile-icon" style="font-size:40px;"></i>
        <div class="profile-menu" id="profileMenu">
          <p><strong>Name:</strong>Madhuri</p>
          <p><strong>Email:</strong> admin@gmail.com</p>
          <p><strong>Role:</strong> Admin</p>
          <a href="/logout">Logout</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Page Layout -->
  <div class="dashboard-container">
    
    <!-- Sidebar -->
    <aside class="sidebar">
      <ul class="sidebar-menu">
        
  <li><a class="nav-link active" onclick="loadPage('/hotelDash', this)"> 🏢 Hotels</a></li>
  <li><a class="nav-link active" onclick="loadPage('/hotelImgDash', this)">🖼️ Hotel Images</a></li>
	<li><a class="nav-link active" onclick="loadPage('/cityDash', this)">🌆 City</a></li>
	<li><a class="nav-link active" onclick="loadPage('/areaDash', this)">📍Area</a></li>
  <li><a class="nav-link active" onclick="loadPage('/areaDash', this)">🛎️ Amenities</a></li>
	<li><a class="nav-link active" onclick="loadPage('viewcustomer', this)">👥 Customer</a></li>
	<li><a class="nav-link active" onclick="loadPage('', this)"> ⭐ Rating & Reviews</a></li>
	<li><a class="nav-link active" onclick="loadPage('', this)">↩ Logout</a></li>
      </ul>

    </aside>
  <main class="main-body">
    
<div class="iframe-wrapper">
      <iframe id="contentFrame" src="" scrolling="no"></iframe>
    </div>
</main>

  </div>

  <!-- JavaScript for Profile Dropdown -->
  <script>
    function toggleProfileMenu() {
      const menu = document.getElementById("profileMenu");
      menu.style.display = menu.style.display === "block" ? "none" : "block";
    }

    // Hide menu on outside click
    document.addEventListener("click", function(event) {
      const profileContainer = document.querySelector(".profile-container");
      const profileMenu = document.getElementById("profileMenu");
      if (!profileContainer.contains(event.target)) {
        profileMenu.style.display = "none";
      }
    });


	function loadPage(page, clickedLink) {
      		document.getElementById('contentFrame').src = page;

      // Handle active class
      document.querySelectorAll('.nav-link').forEach(link => {
        link.classList.remove('active');
      });
      clickedLink.classList.add('active');
    }

  </script>
</body>
</html>